package org.ucombinator.jade.diff

import org.ucombinator.jade.util.Log
import java.io.File
import javax.tools.ToolProvider

object Diff {
  fun main(old: File, new: File) {
    TODO("unimplemented: diff ${old} ${new}")
  }
}
